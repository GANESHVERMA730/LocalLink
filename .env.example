# Backend API base URL — update to your deployed backend URL for production
VITE_API_URL=http://localhost:5000/api

# Backend Socket.io URL — must match the backend origin (same as API without /api)
VITE_SOCKET_URL=http://localhost:5000
