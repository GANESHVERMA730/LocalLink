MONGODB_URI=mongodb+srv://user:pass@cluster.../locallink?retryWrites=true&w=majority
JWT_SECRET=change_this_to_a_long_random_string
PORT=5000
CLIENT_URL=http://localhost:5173
# Set to true to return a reset URL in the forgot-password API (local testing only).
# Production must leave this unset or false — never expose tokens in the API response.
ENABLE_DEV_RESET_LINK=true
